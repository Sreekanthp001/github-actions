name: Dependency
on: [push] #triggers
jobs:
  #job1 and job4 are parellel. no dependency
  job-1: #these should be unique
    runs-on: ubuntu-latest #linux os. we can have windows or mac
    steps: #stages
      - name: step1
        run: echo "Hi, I anm from job-1"
  
  job-2:
    needs: job-1
    runs-on: ubuntu-latest #linux os. we can have windows or mac
    steps: #stages
      - name: step1
        run: echo "Hi, I am from job-2"

  job-3: 
    needs: job-2
    runs-on: ubuntu-latest #linux os. we can have windows or mac
    steps: #stages
      - name: step1
        run: echo "Hi, I am from job-3"

  job-3:
    runs-on: ubuntu-latest #linux os. we can have windows or mac
    stpes: #stages
      - name: step1
        run: echo "Hi, I am from job-4"
